<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@id/navigation_home">

    <fragment
        android:id="@+id/navigation_categoria"
        android:name="com.example.shoptmodaamerican.categoria.view.CategoriaFragment"
        android:label="Categorias"
        tools:layout="@layout/fragment_categorias" />
    <action
        android:id="@+id/action_navigation_home_to_productosPorCategoriaFragment"
        app:destination="@id/productosPorCategoriaFragment"
        app:argumentName="categoriaId"
        app:argType="integer" />

    <fragment
        android:id="@+id/productosPorCategoriaFragment"
        android:name="com.example.shoptmodaamerican.productosporcategoria.view.ProductosPorCategoriaFragment"
        android:label="Productos Por Categoría"
        tools:layout="@layout/fragment_producto_por_categoria" />


    <!-- Fragmento de carrito -->
    <fragment
        android:id="@+id/navigation_carrito"
        android:name="com.example.shoptmodaamerican.carrito.view.CarritoFragment"
        android:label="Carrito"
        tools:layout="@layout/fragment_carrito" />

    <!-- Fragmento de inicio -->
    <fragment
        android:id="@+id/navigation_home"
        android:name="com.example.shoptmodaamerican.inicio.InicioFragment"
        android:label="Inicio"
        tools:layout="@layout/fragment_inicio" />
</navigation>
